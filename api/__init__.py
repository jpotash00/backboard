"""Milestone 2: the FastAPI engine that wraps the interviewer + policy behind the
session API (§4). Session state is in memory (Redis later); every completed transcript
+ outcome is logged as the data asset that compounds."""
